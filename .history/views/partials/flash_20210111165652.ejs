<% if(success && success.length){ %>
    <div role="alert" class="alert alert-success aler-dismissible fade show">
        <%= success %>
    </div>
    <% } %>

        <% if(error && error.length){ %>
            <div role="alert" class="alert alert-warning">
                <%= error %>
            </div>
            <% } %>